---
created: 2026-05-18T20:15:37.421Z
session: cbcc2de1-ad47-45d7-9594-791331cca228
working_directory: /Users/mike.dampier/Development/Payer360
---

## Context

The Provider360 app has a floating chat widget (`ChatWidget.tsx`) powered by a `useChatAgent` hook that streams SSE from a Cortex Agent via `/api/agent`, and renders responses with Vega-Lite charts. It also supports emailing responses and creating Snowflake Alerts directly from chat.

The Payer360 Flask backend already has all three endpoints (`/api/agent`, `/api/email`, `/api/alert`) configured with the correct Payer360 agent URL (`PAYER360_APP.CORTEX_ANALYST.PAYER360_AGENT`). We just need the frontend components and to wire them into `App.tsx`.

## Implementation Steps

1. **Create `src/hooks/useChatAgent.ts`** — Copy from Provider360, change email subject to "Payer 360 Chat Results" (already correct in Flask, but update client-side reference)

2. **Create `src/components/VegaChart.tsx`** — Copy from Provider360 (renders Vega-Lite specs from agent responses with dark theme)

3. **Create `src/components/ChatWidget.tsx`** — Copy from Provider360 with these adaptations:
   - Change title from "Provider Chat" to "Payer Chat"
   - Change sample questions to payer-relevant ones:
     - "What is our current MLR by line of business?"
     - "Which denial categories have the highest volume?"
     - "Show me member renewal trends by plan type"
     - "What is our combined ratio this quarter?"
   - Change alert metric presets to Payer360 metrics:
     - `mlr` → "MLR (%)"
     - `denial_rate` → "Denial Rate (%)"
     - `combined_ratio` → "Combined Ratio (%)"
     - `settlement_days` → "Avg Settlement Days"
     - `renewal_rate` → "Renewal Rate (%)"
     - `network_turnover` → "Network Turnover (%)"
   - Update default alert thresholds accordingly

4. **Update `src/App.tsx`** — Add `import ChatWidget from './components/ChatWidget'` and render `<ChatWidget />` after `</main>` (same position as Provider360)

## Verification

- `npm run build` passes with zero errors
- Chat bubble appears in bottom-right corner
- Clicking opens the chat panel with Payer360 sample questions
- (Full agent test requires the Cortex Agent to be deployed in PAYER360_APP.CORTEX_ANALYST — that's a separate infrastructure step)

## Critical Files

- `/Users/mike.dampier/Development/Provider360/react-app/src/hooks/useChatAgent.ts` — Pattern to follow (SSE parsing)
- `/Users/mike.dampier/Development/Provider360/react-app/src/components/ChatWidget.tsx` — Pattern to follow (UI + alert/email actions)
- `/Users/mike.dampier/Development/Payer360/react-app/src/App.tsx` — Wire ChatWidget into layout
- `/Users/mike.dampier/Development/Provider360/react-app/src/components/VegaChart.tsx` — Vega-Lite renderer to copy